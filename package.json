{
  "name": "kenya-data-atlas",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "geography:build": "node scripts/geography/build-registry.mjs",
    "geography:validate": "node scripts/geography/validate-registry.mjs",
    "catalogue:build": "node scripts/catalogue/build-catalogue.mjs",
    "catalogue:validate": "node scripts/catalogue/validate-catalogue.mjs",
    "build:data": "npm run geography:build && npm run catalogue:build",
    "test": "npm run geography:validate && npm run catalogue:validate"
  }
}

