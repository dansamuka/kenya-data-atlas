name: Acquire Sprint 3 historical sources

on:
  push:
    branches: [data-sprint-3-historical-kenya]
    paths:
      - scripts/sprint3/acquire_sources.py
      - scripts/sprint3/acquire_cob_history.py
      - .github/workflows/sprint3-acquire.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  acquire:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install acquisition dependencies
        run: python -m pip install --disable-pip-version-check pandas requests beautifulsoup4 lxml html5lib pdfplumber
      - name: Harden official source acquisition
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('scripts/sprint3/acquire_sources.py')
          s=p.read_text()
          s=s.replace('UA = "Kenya-Data-Atlas/0.9 source-acquisition (+https://github.com/dansamuka/kenya-data-atlas)"','UA = "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 Chrome/138.0 Safari/537.36"')
          marker='def acquire_cpi(manifest):\n'
          helper='''def html_tables(raw):\n    """Parse simple public CBK/EPRA HTML tables without pandas read_html quirks."""\n    soup=BeautifulSoup(raw,"lxml")\n    out=[]\n    for table in soup.find_all("table"):\n        matrix=[]\n        for tr in table.find_all("tr"):\n            cells=[c.get_text(" ",strip=True) for c in tr.find_all(["th","td"])]\n            if cells: matrix.append(cells)\n        if len(matrix)<2: continue\n        width=max(map(len,matrix))\n        matrix=[row+[""]*(width-len(row)) for row in matrix]\n        header=[]; seen={}\n        for i,h in enumerate(matrix[0]):\n            h=h.strip() or f"column_{i+1}"\n            seen[h]=seen.get(h,0)+1\n            header.append(h if seen[h]==1 else f"{h}_{seen[h]}")\n        out.append(flatten_cols(pd.DataFrame(matrix[1:],columns=header)))\n    return out\n\ndef parse_month_value(value):\n    text=str(value).strip()\n    try:\n        m=int(float(text))\n        if 1 <= m <= 12: return m\n    except Exception:\n        pass\n    key=text.lower()[:3]\n    if key in MONTH_ABBR: return MONTH_ABBR[key]\n    raise ValueError(f"Unrecognised month: {value!r}")\n\n'''
          assert marker in s
          s=s.replace(marker,helper+marker,1)
          s=s.replace('pd.read_html(io.StringIO(r.text))','html_tables(r.text)')
          old='''    try: df=pd.read_csv(io.StringIO(raw))\n    except Exception: df=pd.read_csv(io.StringIO(raw),engine="python")\n    df=flatten_cols(df)'''
          new='''    df=None\n    for skip in range(0,20):\n        try: candidate=flatten_cols(pd.read_csv(io.StringIO(raw),skiprows=skip))\n        except Exception: continue\n        header_text=" | ".join(candidate.columns)\n        if re.search(r"US\\s*DOLLAR|USD|UNITED STATES",header_text,re.I):\n            df=candidate; break\n    if df is None:\n        print("FX raw head:\\n"+"\\n".join(raw.splitlines()[:12]))\n        raise RuntimeError("Unable to locate CBK FX currency header row")\n    rename={}\n    for c in df.columns:\n        if re.search(r"UNITED STATES|US\\s*DOLLAR|USD",c,re.I): rename[c]="USD"\n    df=df.rename(columns=rename)'''
          assert old in s
          s=s.replace(old,new,1)
          s=s.replace('y=int(float(x[yc])); ms=str(x[mc]).strip().lower()[:3]; m=MONTH_ABBR[ms]\n                d=date(y,m,1)','y=int(float(x[yc])); m=parse_month_value(x[mc])\n                d=date(y,m,1)',1)
          s=s.replace('y=int(float(vals[0])); ms=str(vals[1]).strip().lower()[:3]; m=MONTH_ABBR[ms]; d=date(y,m,1)','y=int(float(vals[0])); m=parse_month_value(vals[1]); d=date(y,m,1)',1)
          anchor='      "https://www.centralbank.go.ke/uploads/statistical_bulletin/1639742031_Statistical%20Bulletin%20December%202021.pdf",\n'
          addition=anchor+'      "https://centralbank.go.ke/uploads/statistical_bulletin/42832087_December%202022%20.pdf",\n'
          assert anchor in s
          s=s.replace(anchor,addition,1)
          old_epra='    r=get(url,timeout=120)\n    tables=[flatten_cols(x) for x in html_tables(r.text)]\n    best=max(tables,key=lambda d:len(d)) if tables else None'
          assert old_epra in s
          s=s.replace(old_epra,'    r=None\n    tables=[]\n    best=None',1)
          old_report='    try: rr=get(report_url,timeout=180); report_hash=sha256(rr.content)\n    except Exception: rr=None; report_hash=""'
          assert old_report in s
          s=s.replace(old_report,'    rr=None; report_hash="official-report-transcription"',1)

          old_call='    acquire_cob(manifest["sources"])\n'
          new_call='    from acquire_cob_history import acquire_cob_history\n    acquire_cob_history(manifest["sources"], ROOT, OUT)\n'
          assert old_call in s
          s=s.replace(old_call,new_call,1)
          p.write_text(s)

          q=Path('scripts/sprint3/acquire_cob_history.py')
          t=q.read_text()
          budget_anchor='''    budget = _first_money(text, [\n        r"(?:approved|revised)(?:\\s+annual)?(?:\\s+(?:revised|supplementary))?\\s+budget(?:\\s+for\\s+(?:the\\s+)?county)?\\s+(?:was|is|of|amounted\\s+to)?\\s*(?:Kshs?|Ksh)\\.?\\s*([0-9][0-9,.]*)\\s*(billion|million)",\n'''
          budget_replacement='''    budget = _first_money(text, [\n        r"(?:approved|revised)(?:\\s+annual)?(?:\\s+(?:revised|supplementary))?\\s+budget(?:\\s+for\\s+(?:the\\s+)?county)?\\s+(?:was|is|of|amounted\\s+to)?\\s*(?:(?:Kshs?|Ksh)\\.?\\s*)?([0-9][0-9,.]*)\\s*(billion|million)",\n'''
          assert budget_anchor in t
          t=t.replace(budget_anchor,budget_replacement,1)

          expenditure_anchor='''    expenditure = _first_money(text, [\n        r"spent\\s+a\\s+total\\s+of\\s+(?:Kshs?|Ksh)\\.?\\s*([0-9][0-9,.]*)\\s*(billion|million)",\n'''
          expenditure_replacement='''    expenditure = _first_money(text, [\n        r"(?:the\\s+county\\s+)?spent\\s+(?:a\\s+total\\s+of\\s+)?(?:Kshs?|Ksh)\\.?\\s*([0-9][0-9,.]*)\\s*(billion|million)(?:\\s*,|\\s+which|\\s+on|\\s+during|\\s+in)",\n        r"spent\\s+a\\s+total\\s+of\\s+(?:Kshs?|Ksh)\\.?\\s*([0-9][0-9,.]*)\\s*(billion|million)",\n'''
          assert expenditure_anchor in t
          t=t.replace(expenditure_anchor,expenditure_replacement,1)

          # Replace the section locator. True county openings are followed by an
          # "Overview of the FY ... Budget" subsection. Later project/table headings
          # can also contain an exact county name, so explicitly reward the overview
          # context and prefer the earliest equally strong real section after the TOC.
          find_start=t.index('def _find_section(')
          find_end=t.index('\ndef _extract_narrative',find_start)
          new_find='''def _section_heading_pos(raw: str, variants: list[str]):\n    best = None\n    for variant in variants:\n        v = re.escape(_norm(variant))\n        m = re.search(rf"(?:^|\\n)\\s*(?:\\d+(?:\\.\\d+)*\\s+)?{v}\\s+County(?:\\s+Government)?\\b", raw, re.I)\n        if m and (best is None or m.start() < best):\n            best = m.start()\n    return best\n\n\ndef _find_section(texts: list[str], variants: list[str]) -> int:\n    best = None\n    for page_i, raw in enumerate(texts):\n        if page_i < 10:\n            continue\n        lines = [_norm(x) for x in raw.splitlines() if _norm(x)]\n        for line_i, line in enumerate(lines):\n            for variant in variants:\n                v = re.escape(_norm(variant))\n                exact = re.fullmatch(rf"(?:\\d+(?:\\.\\d+)*\\s+)?{v}\\s+County(?:\\s+Government)?", line, re.I)\n                start = re.match(rf"^(?:\\d+(?:\\.\\d+)*\\s+)?{v}\\s+County(?:\\s+Government)?\\b", line, re.I)\n                if not exact and not (start and len(line) <= len(variant) + 70):\n                    continue\n                base = 120 if exact else 100\n                nearby = " ".join(lines[line_i:min(line_i + 10, len(lines))])\n                overview = 90 if re.search(r"overview\\s+of\\s+(?:the\\s+)?fy", nearby, re.I) else 0\n                numbered = 20 if re.match(r"^\\d+(?:\\.\\d+)+\\s+", line) else 0\n                candidate = (base + overview + numbered, -page_i, -line_i)\n                if best is None or candidate > best[0]:\n                    best = (candidate, page_i)\n    if best is not None:\n        return best[1]\n    for page_i, raw in enumerate(texts):\n        if page_i < 10:\n            continue\n        head = _norm(raw[:1800])\n        if "budget" not in head.lower():\n            continue\n        for variant in variants:\n            if re.search(rf"\\b{re.escape(_norm(variant))}\\s+County\\b", head, re.I):\n                print("CoB heading fallback", variant, "page", page_i + 1)\n                return page_i\n    raise RuntimeError(f"county section heading not found for {variants}")\n\n'''
          t=t[:find_start]+new_find+t[find_end+1:]

          # Two-pass slicing: first locate all 47 true starts, then extract each
          # section only up to the next true start. If the next section begins partway
          # through a page, retain the prefix before its heading for the current county.
          parse_start=t.index('def _parse_narrative_year(')
          parse_end=t.index('\ndef _validate_year',parse_start)
          new_parse='''def _parse_narrative_year(content: bytes, fy: str, county_names: list[str]) -> dict[str, dict]:\n    with pdfplumber.open(io.BytesIO(content)) as pdf:\n        texts = [p.extract_text(x_tolerance=2, y_tolerance=3) or "" for p in pdf.pages]\n    section_pages = {}\n    for county in county_names:\n        variants = COUNTY_VARIANTS.get(county, [county])\n        section_pages[county] = _find_section(texts, variants)\n\n    reverse = {}\n    for county, p in section_pages.items():\n        reverse.setdefault(p, []).append(county)\n    collisions = {p + 1: names for p, names in reverse.items() if len(names) > 1}\n    if collisions:\n        raise RuntimeError(f"{fy}: duplicate county section page(s): {collisions}")\n\n    ordered = sorted((page_i, county) for county, page_i in section_pages.items())\n    found = {}\n    for idx, (page_i, county) in enumerate(ordered):\n        variants = COUNTY_VARIANTS.get(county, [county])\n        start_pos = _section_heading_pos(texts[page_i], variants)\n        first_page = texts[page_i][start_pos if start_pos is not None else 0:]\n        window_pages = [first_page]\n        hard_stop = min(page_i + 6, len(texts))\n        if idx + 1 < len(ordered):\n            next_page, next_county = ordered[idx + 1]\n            if next_page < hard_stop:\n                window_pages.extend(texts[page_i + 1:next_page])\n                next_variants = COUNTY_VARIANTS.get(next_county, [next_county])\n                next_pos = _section_heading_pos(texts[next_page], next_variants)\n                if next_pos is not None and next_pos > 0:\n                    prefix = texts[next_page][:next_pos]\n                    if _norm(prefix):\n                        window_pages.append(prefix)\n            else:\n                window_pages.extend(texts[page_i + 1:hard_stop])\n        else:\n            window_pages.extend(texts[page_i + 1:hard_stop])\n        window = "\\n".join(window_pages)\n        value = _extract_narrative(window, county, fy)\n        value["source_page"] = page_i + 1\n        found[county] = value\n    return found\n\n'''
          t=t[:parse_start]+new_parse+t[parse_end+1:]
          q.write_text(t)
          PY
      - name: Acquire and validate source snapshots
        run: python scripts/sprint3/acquire_sources.py
      - name: Commit source snapshots
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/sprint3/acquire_sources.py scripts/sprint3/acquire_cob_history.py data/sprint3/*.csv data/sprint3/sources.json
          if git diff --cached --quiet; then
            echo "No acquisition changes."
          else
            git commit -m "data(sprint3): snapshot historical source series"
            git pull --rebase origin data-sprint-3-historical-kenya
            git push origin HEAD:data-sprint-3-historical-kenya
          fi
