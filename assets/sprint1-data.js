/* Retired in v0.8.0.
 * Data Sprint 1 is compiled into data/indicators/registry and
 * data/catalogue/registry during npm run build:data.
 * This compatibility file intentionally performs no runtime data mutation.
 */
